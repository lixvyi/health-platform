mysql.url=jdbc:mysql://mysql:3306/health_portal?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai

spring:
  datasource:
    url: ${mysql.url}
    username: ${mysql.username}
    password: ${mysql.password}
    driver-class-name: ${mysql.driver}
  data:
    redis:
      host: redis
      port: 6379

app:
  upload:
    path: /app/uploads
