<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.csu.health.portal.module.auth.mapper.SysUserMapper">

  <!-- 根据用户名查询（讲义：namespace = Dao 完整路径，id = 方法名） -->
  <select id="findByUsername" resultType="com.csu.health.portal.module.auth.entity.SysUser">
    SELECT id, username, password, real_name, role, status, created_at, updated_at
    FROM sys_user
    WHERE username = #{username}
    LIMIT 1
  </select>

</mapper>
